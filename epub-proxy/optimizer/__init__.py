"""Vendored, unmodified from crosspoint-reader/calibre-plugins (MIT license).

See THIRD_PARTY_LICENSES.md in the epub-proxy/ directory for the full
license text and upstream source. This package is CrossPoint Reader's own
EPUB optimizer, reused here so device-optimized EPUBs are produced by the
exact same code path CrossPoint's own web UI and Calibre plugin use --
not a reimplementation we'd have to keep in sync ourselves.
"""
